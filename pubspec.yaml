name: dietapp
description: ""
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.10.7

dependencies:
  flutter:
    sdk: flutter
    
  # UI & Theming
  cupertino_icons: ^1.0.6
  google_fonts: ^6.1.0
  
  # Navigation
  go_router: ^14.0.0
  
  # State Management (da aggiungere quando necessario)
  # provider: ^6.1.1
  # flutter_bloc: ^8.1.3
  # riverpod: ^2.4.9
  
  # HTTP & API (per integrazioni AI future)
  # http: ^1.1.0
  # dio: ^5.4.0
  
  # Local Storage
  # shared_preferences: ^2.2.2
  # hive: ^2.2.3
  # hive_flutter: ^1.1.0
  # sqflite: ^2.3.0
  
  # Utilities
  # intl: ^0.18.1  # Formattazione date/numeri
  # uuid: ^4.2.1   # Generazione ID univoci

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
